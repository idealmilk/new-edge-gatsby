import React from 'react';

import { Container } from './styled';

const PageHeaedr = ({ title }) => {
  return (
    <Container>
      <h1>{title}</h1>
    </Container>
  );
};

export default PageHeaedr;
